# dependencies
node_modules/

# build output
dist/
apps/web/dist/
# rust build artifacts (src-tauri/target игнорится своим .gitignore, эти — новые крейты)
apps/relay-core/target/
apps/relay/target/

# secrets & runtime config (копируй из *.example)
.env*
!.env*.example
livekit.yaml

# vite local env (копируй из .env.local.example)
.env.local
.env.*.local

# database / runtime data
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# logs & os
*.log
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/
